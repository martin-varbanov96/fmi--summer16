#include "stdafx.h"
#include <iostream>
#include "Tree.h"

Tree::Tree()
	:first(NULL), last(NULL){
}
Tree::~Tree(){}
Tree::Tree(const Tree& temp) {
	if (this != &temp) {
		for (each in temp) {
			
		}
	}
}
