#include <iostream>

using namespace std;

int main()
{
    /*


    { if ( ( t = new btree < KeyType >) == NULL)
 { cout << “Няма достатъчно памет!” << endl;
 exit (1);
 }
 t -> left = t -> right = NULL; t -> key = x;
 return;
 }
 if ( x < t -> key ) insert (x, t -> left);
 else insert (x, t -> right);
 }


    nf..
    ..
    }else
    if(x < p->inf) add(x, p->left);
    else add(x, p->right);
    */

    return 0;
}
